<h3>Tipos de Asunto</h3>
<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#newMatterTypeModal">
    Nuevo
</button>
<div ng-controller="mattertypesController as matterTypes">
    <table class="table table-striped table-condensed">
        <thead>
            <th></th>
            <th>#</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Creado</th>
            <th>Modificado</th>
        </thead>
        <tbody>
            <tr ng-repeat="item in items">
                <td class="centered"><a href="#" ng-click="editMatterType(item)" data-toggle="modal" data-target="#editMatterTypeModal"><i class="fa fa-pencil"></i></a></td>
                <td>{{item.Id}}</td>
                <td>{{item.Name}}</td>
                <td>{{item.Description}}</td>
                <td>{{item.relCreateDate}}</td>
                <td>{{item.relModifyDate}}</td>
            </tr>
        </tbody>
    </table>
    @Html.Partial("~/Views/MatterTypes/New.cshtml")
    @Html.Partial("~/Views/MatterTypes/Edit.cshtml")
</div>