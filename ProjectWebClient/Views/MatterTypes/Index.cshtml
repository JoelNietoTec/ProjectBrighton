<h4>Tipos de Asunto</h4>
<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#newMatterTypeModal">
    Nuevo
</button>
<div ng-controller="mattertypesController as matterTypes">
    <table class="table table-hover table-condensed">
        <thead>
            <th class="centered">#</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Creado</th>
            <th>Modificado</th>
            <th class="centered">Comandos</th>
        </thead>
        <tbody>
            <tr ng-repeat="item in items">
                <td class="centered">{{item.Id}}</td>
                <td>{{item.Name}}</td>
                <td>{{item.Description}}</td>
                <td>{{item.relCreateDate}}</td>
                <td>{{item.relModifyDate}}</td>
                <td class="centered commands">
                    <a href="#" ng-click="editMatterType(item)" class="fa-stack edit-command" data-toggle="modal" data-target="#editMatterTypeModal">
                        <i class="fa fa-circle fa-stack-2x"></i>
                        <i class="fa fa-pencil fa-stack-1x fa-inverse"></i>
                    </a>
                    <span class="fa-stack delete-command">
                        <i class="fa fa-circle fa-stack-2x"></i>
                        <i class="fa fa-trash fa-stack-1x fa-inverse"></i>
                    </span>
                </td>
            </tr>
        </tbody>
    </table>
    @Html.Partial("~/Views/MatterTypes/New.cshtml")
    @Html.Partial("~/Views/MatterTypes/Edit.cshtml")
</div>