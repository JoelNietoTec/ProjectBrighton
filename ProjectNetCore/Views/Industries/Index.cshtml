<h3>Industrias</h3>
<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#newIndustryModal">
    Nuevo
</button>
<div ng-controller="industriesController as industries">
    <table class="table table-striped">
        <thead>
            <th></th>
            <th>#</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Creado</th>
            <th>Modificado</th>
        </thead>
        <tbody>
            <tr ng-repeat="item in items">
                <td><a href="#" class="btn btn-sm btn-primary" ng-click="editIndustry(item)" data-toggle="modal" data-target="#editIndustryModal"><i class="fa fa-pencil-square-o"></i></a></td>
                <td>{{item.Id}}</td>
                <td>{{item.Name}}</td>
                <td>{{item.Description}}</td>
                <td>{{item.relCreateDate}}</td>
                <td>{{item.relModifyDate}}</td>
            </tr>
        </tbody>
    </table>
    @Html.Partial("~/Views/Industries/New.cshtml")
    @Html.Partial("~/Views/Industries/Edit.cshtml")
</div>
